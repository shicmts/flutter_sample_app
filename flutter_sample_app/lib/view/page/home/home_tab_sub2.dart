import 'package:flutter/material.dart';

/// 여긴 뭐할까
/// 외부 sdk를 사용할 수 있을까 .. ?
class HomeTabSub2 extends StatelessWidget {
  const HomeTabSub2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
